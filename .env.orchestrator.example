# GitHub Configuration - REQUIRED
GITHUB_TOKEN=your_github_personal_access_token_here
GITHUB_ORG=your-github-organization  # Optional: Use for organization-level runners
GITHUB_REPO=owner/repository-name    # Optional: Use for repository-level runners (alternative to ORG)

# Runner Configuration
RUNNER_IMAGE=ghcr.io/apexcapital/runner:latest
RUNNER_VERSION=2.325.0

# Scaling Configuration
MIN_RUNNERS=2          # Minimum number of runners to always maintain
MAX_RUNNERS=10         # Maximum number of runners allowed
SCALE_UP_THRESHOLD=3   # Queue length that triggers scaling up
SCALE_DOWN_THRESHOLD=1 # Queue length that triggers scaling down
IDLE_TIMEOUT=300       # Seconds before idle runners are terminated

# Monitoring Configuration
POLL_INTERVAL=30       # Seconds between GitHub API polls
LOG_LEVEL=INFO        # DEBUG, INFO, WARNING, ERROR
STRUCTURED_LOGGING=true

# Docker Configuration
RUNNER_NETWORK=runner-network

# Optional: Redis Configuration for distributed setups
# REDIS_URL=redis://redis:6379/0
