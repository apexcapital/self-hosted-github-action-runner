# Runner version (matched to your Dockerfile ARG default)
RUNNER_VERSION=2.325.0

# Name the runner will register under
RUNNER_NAME=my-self-hosted-runner

# GitHub repo or org URL
REPO_URL=https://github.com/<OWNER>/<REPO>

# (DO NOT commit your token here!)
# To generate: GitHub → Settings → Actions → Runners → New self-hosted runner → Generate token
RUNNER_TOKEN=

# Runner work dir (optional; default=_work)
RUNNER_WORKDIR=_work